// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/sqlboiler/v4/types"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// Geofence is an object representing the database table.
type Geofence struct {
	ID            string            `boil:"id" json:"id" toml:"id" yaml:"id"`
	TenantID      string            `boil:"tenant_id" json:"tenant_id" toml:"tenant_id" yaml:"tenant_id"`
	Name          string            `boil:"name" json:"name" toml:"name" yaml:"name"`
	Color         string            `boil:"color" json:"color" toml:"color" yaml:"color"`
	Geometry      types.JSON        `boil:"geometry" json:"geometry" toml:"geometry" yaml:"geometry"`
	AreaM2        float64           `boil:"area_m2" json:"area_m2" toml:"area_m2" yaml:"area_m2"`
	SpeedLimitKPH null.Int          `boil:"speed_limit_kph" json:"speed_limit_kph,omitempty" toml:"speed_limit_kph" yaml:"speed_limit_kph,omitempty"`
	Scope         string            `boil:"scope" json:"scope" toml:"scope" yaml:"scope"`
	GroupIds      types.StringArray `boil:"group_ids" json:"group_ids" toml:"group_ids" yaml:"group_ids"`
	CreatedBy     string            `boil:"created_by" json:"created_by" toml:"created_by" yaml:"created_by"`
	CreatedAt     time.Time         `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt     time.Time         `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *geofenceR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L geofenceL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var GeofenceColumns = struct {
	ID            string
	TenantID      string
	Name          string
	Color         string
	Geometry      string
	AreaM2        string
	SpeedLimitKPH string
	Scope         string
	GroupIds      string
	CreatedBy     string
	CreatedAt     string
	UpdatedAt     string
}{
	ID:            "id",
	TenantID:      "tenant_id",
	Name:          "name",
	Color:         "color",
	Geometry:      "geometry",
	AreaM2:        "area_m2",
	SpeedLimitKPH: "speed_limit_kph",
	Scope:         "scope",
	GroupIds:      "group_ids",
	CreatedBy:     "created_by",
	CreatedAt:     "created_at",
	UpdatedAt:     "updated_at",
}

var GeofenceTableColumns = struct {
	ID            string
	TenantID      string
	Name          string
	Color         string
	Geometry      string
	AreaM2        string
	SpeedLimitKPH string
	Scope         string
	GroupIds      string
	CreatedBy     string
	CreatedAt     string
	UpdatedAt     string
}{
	ID:            "geofences.id",
	TenantID:      "geofences.tenant_id",
	Name:          "geofences.name",
	Color:         "geofences.color",
	Geometry:      "geofences.geometry",
	AreaM2:        "geofences.area_m2",
	SpeedLimitKPH: "geofences.speed_limit_kph",
	Scope:         "geofences.scope",
	GroupIds:      "geofences.group_ids",
	CreatedBy:     "geofences.created_by",
	CreatedAt:     "geofences.created_at",
	UpdatedAt:     "geofences.updated_at",
}

// Generated where

type whereHelpertypes_JSON struct{ field string }

func (w whereHelpertypes_JSON) EQ(x types.JSON) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.EQ, x)
}
func (w whereHelpertypes_JSON) NEQ(x types.JSON) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.NEQ, x)
}
func (w whereHelpertypes_JSON) LT(x types.JSON) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelpertypes_JSON) LTE(x types.JSON) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelpertypes_JSON) GT(x types.JSON) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelpertypes_JSON) GTE(x types.JSON) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}

type whereHelpernull_Int struct{ field string }

func (w whereHelpernull_Int) EQ(x null.Int) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, false, x)
}
func (w whereHelpernull_Int) NEQ(x null.Int) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, true, x)
}
func (w whereHelpernull_Int) LT(x null.Int) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelpernull_Int) LTE(x null.Int) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelpernull_Int) GT(x null.Int) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelpernull_Int) GTE(x null.Int) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}
func (w whereHelpernull_Int) IN(slice []int) qm.QueryMod {
	values := make([]any, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereIn(fmt.Sprintf("%s IN ?", w.field), values...)
}
func (w whereHelpernull_Int) NIN(slice []int) qm.QueryMod {
	values := make([]any, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereNotIn(fmt.Sprintf("%s NOT IN ?", w.field), values...)
}

func (w whereHelpernull_Int) IsNull() qm.QueryMod    { return qmhelper.WhereIsNull(w.field) }
func (w whereHelpernull_Int) IsNotNull() qm.QueryMod { return qmhelper.WhereIsNotNull(w.field) }

type whereHelpertypes_StringArray struct{ field string }

func (w whereHelpertypes_StringArray) EQ(x types.StringArray) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.EQ, x)
}
func (w whereHelpertypes_StringArray) NEQ(x types.StringArray) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.NEQ, x)
}
func (w whereHelpertypes_StringArray) LT(x types.StringArray) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelpertypes_StringArray) LTE(x types.StringArray) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelpertypes_StringArray) GT(x types.StringArray) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelpertypes_StringArray) GTE(x types.StringArray) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}

var GeofenceWhere = struct {
	ID            whereHelperstring
	TenantID      whereHelperstring
	Name          whereHelperstring
	Color         whereHelperstring
	Geometry      whereHelpertypes_JSON
	AreaM2        whereHelperfloat64
	SpeedLimitKPH whereHelpernull_Int
	Scope         whereHelperstring
	GroupIds      whereHelpertypes_StringArray
	CreatedBy     whereHelperstring
	CreatedAt     whereHelpertime_Time
	UpdatedAt     whereHelpertime_Time
}{
	ID:            whereHelperstring{field: "\"geofences\".\"id\""},
	TenantID:      whereHelperstring{field: "\"geofences\".\"tenant_id\""},
	Name:          whereHelperstring{field: "\"geofences\".\"name\""},
	Color:         whereHelperstring{field: "\"geofences\".\"color\""},
	Geometry:      whereHelpertypes_JSON{field: "\"geofences\".\"geometry\""},
	AreaM2:        whereHelperfloat64{field: "\"geofences\".\"area_m2\""},
	SpeedLimitKPH: whereHelpernull_Int{field: "\"geofences\".\"speed_limit_kph\""},
	Scope:         whereHelperstring{field: "\"geofences\".\"scope\""},
	GroupIds:      whereHelpertypes_StringArray{field: "\"geofences\".\"group_ids\""},
	CreatedBy:     whereHelperstring{field: "\"geofences\".\"created_by\""},
	CreatedAt:     whereHelpertime_Time{field: "\"geofences\".\"created_at\""},
	UpdatedAt:     whereHelpertime_Time{field: "\"geofences\".\"updated_at\""},
}

// GeofenceRels is where relationship names are stored.
var GeofenceRels = struct {
	Tenant                string
	GeofencePasses        string
	GeofenceScanCoverages string
	VehicleGeofences      string
}{
	Tenant:                "Tenant",
	GeofencePasses:        "GeofencePasses",
	GeofenceScanCoverages: "GeofenceScanCoverages",
	VehicleGeofences:      "VehicleGeofences",
}

// geofenceR is where relationships are stored.
type geofenceR struct {
	Tenant                *Tenant                   `boil:"Tenant" json:"Tenant" toml:"Tenant" yaml:"Tenant"`
	GeofencePasses        GeofencePassSlice         `boil:"GeofencePasses" json:"GeofencePasses" toml:"GeofencePasses" yaml:"GeofencePasses"`
	GeofenceScanCoverages GeofenceScanCoverageSlice `boil:"GeofenceScanCoverages" json:"GeofenceScanCoverages" toml:"GeofenceScanCoverages" yaml:"GeofenceScanCoverages"`
	VehicleGeofences      VehicleGeofenceSlice      `boil:"VehicleGeofences" json:"VehicleGeofences" toml:"VehicleGeofences" yaml:"VehicleGeofences"`
}

// NewStruct creates a new relationship struct
func (*geofenceR) NewStruct() *geofenceR {
	return &geofenceR{}
}

func (o *Geofence) GetTenant() *Tenant {
	if o == nil {
		return nil
	}

	return o.R.GetTenant()
}

func (r *geofenceR) GetTenant() *Tenant {
	if r == nil {
		return nil
	}

	return r.Tenant
}

func (o *Geofence) GetGeofencePasses() GeofencePassSlice {
	if o == nil {
		return nil
	}

	return o.R.GetGeofencePasses()
}

func (r *geofenceR) GetGeofencePasses() GeofencePassSlice {
	if r == nil {
		return nil
	}

	return r.GeofencePasses
}

func (o *Geofence) GetGeofenceScanCoverages() GeofenceScanCoverageSlice {
	if o == nil {
		return nil
	}

	return o.R.GetGeofenceScanCoverages()
}

func (r *geofenceR) GetGeofenceScanCoverages() GeofenceScanCoverageSlice {
	if r == nil {
		return nil
	}

	return r.GeofenceScanCoverages
}

func (o *Geofence) GetVehicleGeofences() VehicleGeofenceSlice {
	if o == nil {
		return nil
	}

	return o.R.GetVehicleGeofences()
}

func (r *geofenceR) GetVehicleGeofences() VehicleGeofenceSlice {
	if r == nil {
		return nil
	}

	return r.VehicleGeofences
}

// geofenceL is where Load methods for each relationship are stored.
type geofenceL struct{}

var (
	geofenceAllColumns            = []string{"id", "tenant_id", "name", "color", "geometry", "area_m2", "speed_limit_kph", "scope", "group_ids", "created_by", "created_at", "updated_at"}
	geofenceColumnsWithoutDefault = []string{"id", "tenant_id", "name", "color", "geometry", "created_by"}
	geofenceColumnsWithDefault    = []string{"area_m2", "speed_limit_kph", "scope", "group_ids", "created_at", "updated_at"}
	geofencePrimaryKeyColumns     = []string{"id"}
	geofenceGeneratedColumns      = []string{}
)

type (
	// GeofenceSlice is an alias for a slice of pointers to Geofence.
	// This should almost always be used instead of []Geofence.
	GeofenceSlice []*Geofence
	// GeofenceHook is the signature for custom Geofence hook methods
	GeofenceHook func(context.Context, boil.ContextExecutor, *Geofence) error

	geofenceQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	geofenceType                 = reflect.TypeOf(&Geofence{})
	geofenceMapping              = queries.MakeStructMapping(geofenceType)
	geofencePrimaryKeyMapping, _ = queries.BindMapping(geofenceType, geofenceMapping, geofencePrimaryKeyColumns)
	geofenceInsertCacheMut       sync.RWMutex
	geofenceInsertCache          = make(map[string]insertCache)
	geofenceUpdateCacheMut       sync.RWMutex
	geofenceUpdateCache          = make(map[string]updateCache)
	geofenceUpsertCacheMut       sync.RWMutex
	geofenceUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var geofenceAfterSelectMu sync.Mutex
var geofenceAfterSelectHooks []GeofenceHook

var geofenceBeforeInsertMu sync.Mutex
var geofenceBeforeInsertHooks []GeofenceHook
var geofenceAfterInsertMu sync.Mutex
var geofenceAfterInsertHooks []GeofenceHook

var geofenceBeforeUpdateMu sync.Mutex
var geofenceBeforeUpdateHooks []GeofenceHook
var geofenceAfterUpdateMu sync.Mutex
var geofenceAfterUpdateHooks []GeofenceHook

var geofenceBeforeDeleteMu sync.Mutex
var geofenceBeforeDeleteHooks []GeofenceHook
var geofenceAfterDeleteMu sync.Mutex
var geofenceAfterDeleteHooks []GeofenceHook

var geofenceBeforeUpsertMu sync.Mutex
var geofenceBeforeUpsertHooks []GeofenceHook
var geofenceAfterUpsertMu sync.Mutex
var geofenceAfterUpsertHooks []GeofenceHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *Geofence) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range geofenceAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *Geofence) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range geofenceBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *Geofence) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range geofenceAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *Geofence) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range geofenceBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *Geofence) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range geofenceAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *Geofence) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range geofenceBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *Geofence) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range geofenceAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *Geofence) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range geofenceBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *Geofence) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range geofenceAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddGeofenceHook registers your hook function for all future operations.
func AddGeofenceHook(hookPoint boil.HookPoint, geofenceHook GeofenceHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		geofenceAfterSelectMu.Lock()
		geofenceAfterSelectHooks = append(geofenceAfterSelectHooks, geofenceHook)
		geofenceAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		geofenceBeforeInsertMu.Lock()
		geofenceBeforeInsertHooks = append(geofenceBeforeInsertHooks, geofenceHook)
		geofenceBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		geofenceAfterInsertMu.Lock()
		geofenceAfterInsertHooks = append(geofenceAfterInsertHooks, geofenceHook)
		geofenceAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		geofenceBeforeUpdateMu.Lock()
		geofenceBeforeUpdateHooks = append(geofenceBeforeUpdateHooks, geofenceHook)
		geofenceBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		geofenceAfterUpdateMu.Lock()
		geofenceAfterUpdateHooks = append(geofenceAfterUpdateHooks, geofenceHook)
		geofenceAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		geofenceBeforeDeleteMu.Lock()
		geofenceBeforeDeleteHooks = append(geofenceBeforeDeleteHooks, geofenceHook)
		geofenceBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		geofenceAfterDeleteMu.Lock()
		geofenceAfterDeleteHooks = append(geofenceAfterDeleteHooks, geofenceHook)
		geofenceAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		geofenceBeforeUpsertMu.Lock()
		geofenceBeforeUpsertHooks = append(geofenceBeforeUpsertHooks, geofenceHook)
		geofenceBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		geofenceAfterUpsertMu.Lock()
		geofenceAfterUpsertHooks = append(geofenceAfterUpsertHooks, geofenceHook)
		geofenceAfterUpsertMu.Unlock()
	}
}

// One returns a single geofence record from the query.
func (q geofenceQuery) One(ctx context.Context, exec boil.ContextExecutor) (*Geofence, error) {
	o := &Geofence{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for geofences")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all Geofence records from the query.
func (q geofenceQuery) All(ctx context.Context, exec boil.ContextExecutor) (GeofenceSlice, error) {
	var o []*Geofence

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to Geofence slice")
	}

	if len(geofenceAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all Geofence records in the query.
func (q geofenceQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count geofences rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q geofenceQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if geofences exists")
	}

	return count > 0, nil
}

// Tenant pointed to by the foreign key.
func (o *Geofence) Tenant(mods ...qm.QueryMod) tenantQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.TenantID),
	}

	queryMods = append(queryMods, mods...)

	return Tenants(queryMods...)
}

// GeofencePasses retrieves all the geofence_pass's GeofencePasses with an executor.
func (o *Geofence) GeofencePasses(mods ...qm.QueryMod) geofencePassQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"geofence_passes\".\"geofence_id\"=?", o.ID),
	)

	return GeofencePasses(queryMods...)
}

// GeofenceScanCoverages retrieves all the geofence_scan_coverage's GeofenceScanCoverages with an executor.
func (o *Geofence) GeofenceScanCoverages(mods ...qm.QueryMod) geofenceScanCoverageQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"geofence_scan_coverage\".\"geofence_id\"=?", o.ID),
	)

	return GeofenceScanCoverages(queryMods...)
}

// VehicleGeofences retrieves all the vehicle_geofence's VehicleGeofences with an executor.
func (o *Geofence) VehicleGeofences(mods ...qm.QueryMod) vehicleGeofenceQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"vehicle_geofences\".\"geofence_id\"=?", o.ID),
	)

	return VehicleGeofences(queryMods...)
}

// LoadTenant allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (geofenceL) LoadTenant(ctx context.Context, e boil.ContextExecutor, singular bool, maybeGeofence any, mods queries.Applicator) error {
	var slice []*Geofence
	var object *Geofence

	if singular {
		var ok bool
		object, ok = maybeGeofence.(*Geofence)
		if !ok {
			object = new(Geofence)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeGeofence)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeGeofence))
			}
		}
	} else {
		s, ok := maybeGeofence.(*[]*Geofence)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeGeofence)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeGeofence))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &geofenceR{}
		}
		args[object.TenantID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &geofenceR{}
			}

			args[obj.TenantID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`tenants`),
		qm.WhereIn(`tenants.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Tenant")
	}

	var resultSlice []*Tenant
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Tenant")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for tenants")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for tenants")
	}

	if len(tenantAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Tenant = foreign
		if foreign.R == nil {
			foreign.R = &tenantR{}
		}
		foreign.R.Geofences = append(foreign.R.Geofences, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.TenantID == foreign.ID {
				local.R.Tenant = foreign
				if foreign.R == nil {
					foreign.R = &tenantR{}
				}
				foreign.R.Geofences = append(foreign.R.Geofences, local)
				break
			}
		}
	}

	return nil
}

// LoadGeofencePasses allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (geofenceL) LoadGeofencePasses(ctx context.Context, e boil.ContextExecutor, singular bool, maybeGeofence any, mods queries.Applicator) error {
	var slice []*Geofence
	var object *Geofence

	if singular {
		var ok bool
		object, ok = maybeGeofence.(*Geofence)
		if !ok {
			object = new(Geofence)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeGeofence)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeGeofence))
			}
		}
	} else {
		s, ok := maybeGeofence.(*[]*Geofence)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeGeofence)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeGeofence))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &geofenceR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &geofenceR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`geofence_passes`),
		qm.WhereIn(`geofence_passes.geofence_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load geofence_passes")
	}

	var resultSlice []*GeofencePass
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice geofence_passes")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on geofence_passes")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for geofence_passes")
	}

	if len(geofencePassAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.GeofencePasses = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &geofencePassR{}
			}
			foreign.R.Geofence = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.GeofenceID {
				local.R.GeofencePasses = append(local.R.GeofencePasses, foreign)
				if foreign.R == nil {
					foreign.R = &geofencePassR{}
				}
				foreign.R.Geofence = local
				break
			}
		}
	}

	return nil
}

// LoadGeofenceScanCoverages allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (geofenceL) LoadGeofenceScanCoverages(ctx context.Context, e boil.ContextExecutor, singular bool, maybeGeofence any, mods queries.Applicator) error {
	var slice []*Geofence
	var object *Geofence

	if singular {
		var ok bool
		object, ok = maybeGeofence.(*Geofence)
		if !ok {
			object = new(Geofence)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeGeofence)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeGeofence))
			}
		}
	} else {
		s, ok := maybeGeofence.(*[]*Geofence)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeGeofence)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeGeofence))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &geofenceR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &geofenceR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`geofence_scan_coverage`),
		qm.WhereIn(`geofence_scan_coverage.geofence_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load geofence_scan_coverage")
	}

	var resultSlice []*GeofenceScanCoverage
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice geofence_scan_coverage")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on geofence_scan_coverage")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for geofence_scan_coverage")
	}

	if len(geofenceScanCoverageAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.GeofenceScanCoverages = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &geofenceScanCoverageR{}
			}
			foreign.R.Geofence = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.GeofenceID {
				local.R.GeofenceScanCoverages = append(local.R.GeofenceScanCoverages, foreign)
				if foreign.R == nil {
					foreign.R = &geofenceScanCoverageR{}
				}
				foreign.R.Geofence = local
				break
			}
		}
	}

	return nil
}

// LoadVehicleGeofences allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (geofenceL) LoadVehicleGeofences(ctx context.Context, e boil.ContextExecutor, singular bool, maybeGeofence any, mods queries.Applicator) error {
	var slice []*Geofence
	var object *Geofence

	if singular {
		var ok bool
		object, ok = maybeGeofence.(*Geofence)
		if !ok {
			object = new(Geofence)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeGeofence)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeGeofence))
			}
		}
	} else {
		s, ok := maybeGeofence.(*[]*Geofence)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeGeofence)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeGeofence))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &geofenceR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &geofenceR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`vehicle_geofences`),
		qm.WhereIn(`vehicle_geofences.geofence_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load vehicle_geofences")
	}

	var resultSlice []*VehicleGeofence
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice vehicle_geofences")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on vehicle_geofences")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for vehicle_geofences")
	}

	if len(vehicleGeofenceAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.VehicleGeofences = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &vehicleGeofenceR{}
			}
			foreign.R.Geofence = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.GeofenceID {
				local.R.VehicleGeofences = append(local.R.VehicleGeofences, foreign)
				if foreign.R == nil {
					foreign.R = &vehicleGeofenceR{}
				}
				foreign.R.Geofence = local
				break
			}
		}
	}

	return nil
}

// SetTenant of the geofence to the related item.
// Sets o.R.Tenant to related.
// Adds o to related.R.Geofences.
func (o *Geofence) SetTenant(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Tenant) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"geofences\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"tenant_id"}),
		strmangle.WhereClause("\"", "\"", 2, geofencePrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.TenantID = related.ID
	if o.R == nil {
		o.R = &geofenceR{
			Tenant: related,
		}
	} else {
		o.R.Tenant = related
	}

	if related.R == nil {
		related.R = &tenantR{
			Geofences: GeofenceSlice{o},
		}
	} else {
		related.R.Geofences = append(related.R.Geofences, o)
	}

	return nil
}

// AddGeofencePasses adds the given related objects to the existing relationships
// of the geofence, optionally inserting them as new records.
// Appends related to o.R.GeofencePasses.
// Sets related.R.Geofence appropriately.
func (o *Geofence) AddGeofencePasses(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*GeofencePass) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.GeofenceID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"geofence_passes\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"geofence_id"}),
				strmangle.WhereClause("\"", "\"", 2, geofencePassPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.GeofenceID, rel.TokenID, rel.EnteredAt}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.GeofenceID = o.ID
		}
	}

	if o.R == nil {
		o.R = &geofenceR{
			GeofencePasses: related,
		}
	} else {
		o.R.GeofencePasses = append(o.R.GeofencePasses, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &geofencePassR{
				Geofence: o,
			}
		} else {
			rel.R.Geofence = o
		}
	}
	return nil
}

// AddGeofenceScanCoverages adds the given related objects to the existing relationships
// of the geofence, optionally inserting them as new records.
// Appends related to o.R.GeofenceScanCoverages.
// Sets related.R.Geofence appropriately.
func (o *Geofence) AddGeofenceScanCoverages(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*GeofenceScanCoverage) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.GeofenceID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"geofence_scan_coverage\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"geofence_id"}),
				strmangle.WhereClause("\"", "\"", 2, geofenceScanCoveragePrimaryKeyColumns),
			)
			values := []any{o.ID, rel.GeofenceID, rel.TokenID, rel.ScannedFrom}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.GeofenceID = o.ID
		}
	}

	if o.R == nil {
		o.R = &geofenceR{
			GeofenceScanCoverages: related,
		}
	} else {
		o.R.GeofenceScanCoverages = append(o.R.GeofenceScanCoverages, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &geofenceScanCoverageR{
				Geofence: o,
			}
		} else {
			rel.R.Geofence = o
		}
	}
	return nil
}

// AddVehicleGeofences adds the given related objects to the existing relationships
// of the geofence, optionally inserting them as new records.
// Appends related to o.R.VehicleGeofences.
// Sets related.R.Geofence appropriately.
func (o *Geofence) AddVehicleGeofences(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*VehicleGeofence) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.GeofenceID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"vehicle_geofences\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"geofence_id"}),
				strmangle.WhereClause("\"", "\"", 2, vehicleGeofencePrimaryKeyColumns),
			)
			values := []any{o.ID, rel.TenantID, rel.TokenID, rel.GeofenceID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.GeofenceID = o.ID
		}
	}

	if o.R == nil {
		o.R = &geofenceR{
			VehicleGeofences: related,
		}
	} else {
		o.R.VehicleGeofences = append(o.R.VehicleGeofences, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &vehicleGeofenceR{
				Geofence: o,
			}
		} else {
			rel.R.Geofence = o
		}
	}
	return nil
}

// Geofences retrieves all the records using an executor.
func Geofences(mods ...qm.QueryMod) geofenceQuery {
	mods = append(mods, qm.From("\"geofences\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"geofences\".*"})
	}

	return geofenceQuery{q}
}

// FindGeofence retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindGeofence(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*Geofence, error) {
	geofenceObj := &Geofence{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"geofences\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, geofenceObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from geofences")
	}

	if err = geofenceObj.doAfterSelectHooks(ctx, exec); err != nil {
		return geofenceObj, err
	}

	return geofenceObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *Geofence) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no geofences provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(geofenceColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	geofenceInsertCacheMut.RLock()
	cache, cached := geofenceInsertCache[key]
	geofenceInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			geofenceAllColumns,
			geofenceColumnsWithDefault,
			geofenceColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(geofenceType, geofenceMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(geofenceType, geofenceMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"geofences\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"geofences\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into geofences")
	}

	if !cached {
		geofenceInsertCacheMut.Lock()
		geofenceInsertCache[key] = cache
		geofenceInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the Geofence.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *Geofence) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	geofenceUpdateCacheMut.RLock()
	cache, cached := geofenceUpdateCache[key]
	geofenceUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			geofenceAllColumns,
			geofencePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update geofences, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"geofences\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, geofencePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(geofenceType, geofenceMapping, append(wl, geofencePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update geofences row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for geofences")
	}

	if !cached {
		geofenceUpdateCacheMut.Lock()
		geofenceUpdateCache[key] = cache
		geofenceUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q geofenceQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for geofences")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for geofences")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o GeofenceSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), geofencePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"geofences\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, geofencePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in geofence slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all geofence")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *Geofence) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("models: no geofences provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(geofenceColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	geofenceUpsertCacheMut.RLock()
	cache, cached := geofenceUpsertCache[key]
	geofenceUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			geofenceAllColumns,
			geofenceColumnsWithDefault,
			geofenceColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			geofenceAllColumns,
			geofencePrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert geofences, could not build update column list")
		}

		ret := strmangle.SetComplement(geofenceAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(geofencePrimaryKeyColumns) == 0 {
				return errors.New("models: unable to upsert geofences, could not build conflict column list")
			}

			conflict = make([]string, len(geofencePrimaryKeyColumns))
			copy(conflict, geofencePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"geofences\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(geofenceType, geofenceMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(geofenceType, geofenceMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert geofences")
	}

	if !cached {
		geofenceUpsertCacheMut.Lock()
		geofenceUpsertCache[key] = cache
		geofenceUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single Geofence record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *Geofence) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no Geofence provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), geofencePrimaryKeyMapping)
	sql := "DELETE FROM \"geofences\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from geofences")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for geofences")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q geofenceQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no geofenceQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from geofences")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for geofences")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o GeofenceSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(geofenceBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), geofencePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"geofences\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, geofencePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from geofence slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for geofences")
	}

	if len(geofenceAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *Geofence) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindGeofence(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *GeofenceSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := GeofenceSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), geofencePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"geofences\".* FROM \"geofences\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, geofencePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in GeofenceSlice")
	}

	*o = slice

	return nil
}

// GeofenceExists checks if the Geofence row exists.
func GeofenceExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"geofences\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if geofences exists")
	}

	return exists, nil
}

// Exists checks if the Geofence row exists.
func (o *Geofence) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return GeofenceExists(ctx, exec, o.ID)
}
