# charts/

Placeholder. No Helm chart yet — the `api/` backend hasn't been implemented,
so there's nothing to deploy. When it lands, expect a `fleet-lite-app/` chart
mirroring `rental-fleets-app/charts/rental-fleets-app/`:

- `Chart.yaml`
- `values.yaml`
- `values-prod.yaml`
- `templates/`

Deployed via Argo CD to the DIMO Kubernetes cluster.
