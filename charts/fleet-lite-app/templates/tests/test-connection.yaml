apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fleet-lite-app.fullname" . }}-test-connection"
  labels:
    {{- include "fleet-lite-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fleet-lite-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
